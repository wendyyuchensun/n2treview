class RandomBlock {
  field Block newRandonBlock;
  function void randomX() {
    var int xIndex;
    return xIndex; 
  }
  function void randomY() {
    var int yIndex;
    return yIndex; 
  } 
  constructor RandomBlcok new(Snake snake) {
    var int xIndex, yIndex;
    let xIndex = RandomBlock.randomX();
    let yIndex = RandomBlock.randonY();
    let randomBlock = Block.new(xIndex, yIndex);
    return this;
  }
}
